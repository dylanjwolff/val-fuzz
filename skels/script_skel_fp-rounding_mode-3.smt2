(set-logic ALL)
(declare-const BAV1 Bool)
(declare-fun rm1 () (RoundingMode))
(declare-fun rm2 () (RoundingMode))
(assert (not (= rm1 rm2)))
(assert (= BAV1 (= rm1 rm2)))
(check-sat)
(get-model)
(check-sat-using smt)