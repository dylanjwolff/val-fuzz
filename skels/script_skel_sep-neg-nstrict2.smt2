(set-logic ALL)
(declare-const BAV1 Bool)
(declare-const x Int)
(declare-const y Int)
(declare-const z Int)
(declare-const a Int)
(declare-const b Int)
(assert (not (= a b)))
(assert (not (sep true (pto x b))))
(assert (sep (pto x a) (pto z b)))
(assert (= BAV1 (= a b)))
(check-sat)
(get-model)