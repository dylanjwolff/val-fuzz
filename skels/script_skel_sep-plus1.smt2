(set-logic ALL)
(declare-const BAV1 Bool)
(declare-const BAV2 Bool)
(declare-const GEN1 Int)
(declare-const x Int)
(declare-const y Int)
(declare-const z Int)
(declare-const a Int)
(declare-const b Int)
(declare-const c Int)
(assert (and (sep (pto x a) true) (sep (pto y (+ a 1)) true)))
(assert (= x y))
(assert (= BAV1 (and (sep (pto x a) true) (sep (pto y (+ a 1)) true))))
(assert (= BAV2 (= x y)))
(check-sat)
(get-model)