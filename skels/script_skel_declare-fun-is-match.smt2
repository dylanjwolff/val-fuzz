(set-info :smt-lib-version 2.6)
(set-logic ALL)
(declare-const BAV1 Bool)
(declare-fun match (Int Int) Int)
(declare-fun is (Int Int) Int)
(assert (= match is))
(assert (= BAV1 (= match is)))
(check-sat)
(get-model)
(exit)