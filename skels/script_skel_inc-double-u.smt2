(set-logic ALL)
(declare-const BAV1 Bool)
(get-model)
(declare-const GEN1 Int)
(declare-fun P (Int) Bool)
(declare-fun R (Int) Bool)
(assert (forall ((x Int)) (=> (R x) (not (P x)))))
(assert (= BAV1 (forall ((x Int)) (=> (R x) (not (P x))))))
(check-sat)
(get-model)
(assert (R GEN1))
(check-sat)
(assert (forall ((x Int)) (P x)))
(check-sat)
(push 1)
(check-sat)