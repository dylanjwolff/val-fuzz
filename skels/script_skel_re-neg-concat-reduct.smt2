(set-info :smt-lib-version 2.5)
(set-logic ALL)
(declare-const BAV1 Bool)
(declare-const GEN5 String)
(declare-const GEN4 String)
(declare-const GEN3 String)
(declare-const GEN2 String)
(declare-const GEN1 String)
(set-option :strings-exp true)
(declare-fun x () String)
(assert (not (= x GEN1)))
(assert (not (str.in.re x (re.++ (str.to.re GEN2) (re.* (str.to.re GEN3))))))
(assert (not (str.in.re x (re.++ (re.* (str.to.re GEN4)) (str.to.re GEN5)))))
(assert (= BAV1 (= x GEN1)))
(check-sat)
(get-model)