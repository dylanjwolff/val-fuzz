(set-logic ALL)
(declare-const BAV1 Bool)
(declare-const GEN2 Int)
(declare-const GEN1 Int)
(declare-fun S () (Set Int))
(assert (>= (card S) GEN1))
(assert (not (member GEN2 S)))
(assert (= BAV1 (>= (card S) GEN1)))
(check-sat)
(get-model)