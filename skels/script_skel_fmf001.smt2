(set-info :smt-lib-version 2.5)
(set-logic ALL)
(declare-const BAV1 Bool)
(declare-const BAV2 Bool)
(declare-const GEN4 String)
(declare-const GEN3 String)
(declare-const GEN2 String)
(declare-const GEN1 String)
(set-option :strings-exp true)
(set-option :strings-fmf true)
(declare-fun x () String)
(declare-fun y () String)
(assert (str.in.re x (re.* (re.++ (re.* (str.to.re GEN1)) (str.to.re GEN2)))))
(assert (str.in.re y (re.* (re.++ (re.* (str.to.re GEN3)) (str.to.re GEN4)))))
(assert (not (= x y)))
(assert (= (str.len x) (str.len y)))
(assert (= BAV1 (= x y)))
(assert (= BAV2 (= (str.len x) (str.len y))))
(check-sat)
(get-model)