(set-logic ALL)
(declare-const BAV1 Bool)
(declare-const GEN1 Int)
(set-option :sets-ext true)
(declare-datatype Unit ((mkUnit)))
(declare-fun S () (Set (Array Int Unit)))
(assert (> (card S) GEN1))
(assert (= BAV1 (> (card S) GEN1)))
(check-sat)
(get-model)